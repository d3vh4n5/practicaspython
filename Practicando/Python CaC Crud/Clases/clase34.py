# instalamos seaborn desde el cmd, usando el comando "pip install seaborn"
# haciendo un "pip list" desde el cmd podemos ver todas las cosas que tenemos instaladas

import pandas as pd
import seaborn as sns

